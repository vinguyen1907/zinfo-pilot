# Chat Agent — Plan 1: UI, API & Storage

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the full chat UI, FastAPI backend (chat endpoint + conversation history), and SQLite storage — independently runnable with a stub retriever, no Confluence credentials required.

**Architecture:** FastAPI serves a single `frontend/index.html` as a static file. `POST /chat` accepts `{email, message}`, calls a stub retriever that returns fake chunks, streams a canned SSE response, and persists the exchange to SQLite. `GET /conversations/{email}` returns history. The stub retriever is a drop-in interface; Plan 2 replaces it with the real RAG implementation.

**Tech Stack:** Python 3.11+, FastAPI, uvicorn, aiosqlite, python-dotenv, pydantic

**Integration contract with Plan 2:** Plan 2 will create `backend/retriever.py` with a `retrieve(query: str, email: str, top_k: int = 5) -> list[dict]` function where each dict has keys `document: str` and `metadata: dict` (with `page_id`, `page_title`, `page_url`). When Plan 2 is done, change the one import line in `main.py` from `backend.retriever_stub` to `backend.retriever`.

---

## File Map

| Path | Responsibility |
|---|---|
| `requirements.txt` | Minimal deps for Plan 1 |
| `.env.example` | Env var template |
| `backend/__init__.py` | Package marker |
| `backend/db.py` | SQLite init, `save_message`, `get_history` |
| `backend/retriever_stub.py` | Stub `retrieve()` returning hardcoded fake chunks |
| `backend/main.py` | FastAPI app: `/chat` (SSE), `/conversations/{email}`, static file mount |
| `frontend/index.html` | Email gate + chat screen, SSE streaming, citation pills |
| `tests/__init__.py` | Package marker |
| `tests/test_db.py` | Unit tests for db.py |

---

## Task 1: Project scaffold

**Files:**
- Create: `requirements.txt`
- Create: `.env.example`
- Create: `backend/__init__.py`
- Create: `tests/__init__.py`
- Create: `data/` (directory, gitignored)
- Create: `frontend/` (directory placeholder)

- [ ] **Step 1: Create `requirements.txt`**

```
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
aiosqlite==0.20.0
pydantic==2.7.1
```

- [ ] **Step 2: Create `.env.example`**

```
# Plan 1 — no Confluence or LLM credentials needed yet
DATABASE_PATH=./data/conversations.db

# Plan 2 will add:
# CONFLUENCE_BASE_URL=https://yourorg.atlassian.net
# CONFLUENCE_ADMIN_TOKEN=...
# CONFLUENCE_ADMIN_EMAIL=...
# CONFLUENCE_SPACE_KEYS=DEV,INFRA,ONBOARD
# CONFLUENCE_WEBHOOK_SECRET=...
# AGENTBASE_API_KEY=...
# AGENTBASE_MODEL_PATH=qwen/qwen2.5-72b-instruct
# CHROMA_PATH=./chroma_db
```

- [ ] **Step 3: Create directories and init files**

```bash
mkdir -p backend tests frontend data
touch backend/__init__.py tests/__init__.py
echo "data/" >> .gitignore
echo "chroma_db/" >> .gitignore
echo ".env" >> .gitignore
```

- [ ] **Step 4: Install dependencies**

```bash
pip install -r requirements.txt
```

Expected: all packages install without error.

- [ ] **Step 5: Create `.env` from template**

```bash
cp .env.example .env
```

- [ ] **Step 6: Commit**

```bash
git add requirements.txt .env.example .gitignore backend/__init__.py tests/__init__.py
git commit -m "chore: scaffold project structure and Plan 1 dependencies"
```

---

## Task 2: SQLite conversation store (`backend/db.py`)

**Files:**
- Create: `backend/db.py`
- Create: `tests/test_db.py`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_db.py
import asyncio, os, pytest
os.environ["DATABASE_PATH"] = ":memory:"

from backend.db import init_db, save_message, get_history

@pytest.fixture(autouse=True)
async def setup():
    await init_db()

@pytest.mark.asyncio
async def test_save_and_retrieve():
    await save_message("alice@x.com", "user", "hello", None)
    await save_message("alice@x.com", "assistant", "hi", [{"title": "Pg", "url": "http://x"}])
    rows = await get_history("alice@x.com", limit=50)
    assert len(rows) == 2
    assert rows[0]["role"] == "user"
    assert rows[0]["content"] == "hello"
    assert rows[1]["citations"] == [{"title": "Pg", "url": "http://x"}]

@pytest.mark.asyncio
async def test_limit_is_respected():
    for i in range(15):
        await save_message("bob@x.com", "user", f"msg {i}", None)
    rows = await get_history("bob@x.com", limit=10)
    assert len(rows) == 10

@pytest.mark.asyncio
async def test_emails_are_isolated():
    await save_message("a@x.com", "user", "a msg", None)
    await save_message("b@x.com", "user", "b msg", None)
    assert len(await get_history("a@x.com", limit=50)) == 1
    assert len(await get_history("b@x.com", limit=50)) == 1

@pytest.mark.asyncio
async def test_null_citations_stored_as_none():
    await save_message("c@x.com", "user", "question", None)
    rows = await get_history("c@x.com", limit=10)
    assert rows[0]["citations"] is None
```

- [ ] **Step 2: Run — expect failure**

```bash
pytest tests/test_db.py -v
```

Expected: `ModuleNotFoundError: No module named 'backend.db'`

- [ ] **Step 3: Implement `backend/db.py`**

```python
import json, os
import aiosqlite
from dotenv import load_dotenv

load_dotenv()

DB_PATH = os.getenv("DATABASE_PATH", "./data/conversations.db")

_CREATE_SQL = """
CREATE TABLE IF NOT EXISTS conversations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL,
    role TEXT NOT NULL,
    content TEXT NOT NULL,
    citations_json TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_email_created ON conversations(email, created_at);
"""

async def init_db():
    db_dir = os.path.dirname(DB_PATH)
    if db_dir:
        os.makedirs(db_dir, exist_ok=True)
    async with aiosqlite.connect(DB_PATH) as conn:
        await conn.executescript(_CREATE_SQL)
        await conn.commit()

async def save_message(email: str, role: str, content: str, citations: list | None):
    async with aiosqlite.connect(DB_PATH) as conn:
        await conn.execute(
            "INSERT INTO conversations (email, role, content, citations_json) VALUES (?,?,?,?)",
            (email, role, content, json.dumps(citations) if citations else None),
        )
        await conn.commit()

async def get_history(email: str, limit: int = 50) -> list[dict]:
    async with aiosqlite.connect(DB_PATH) as conn:
        conn.row_factory = aiosqlite.Row
        async with conn.execute(
            "SELECT role, content, citations_json, created_at "
            "FROM conversations WHERE email=? ORDER BY created_at ASC LIMIT ?",
            (email, limit),
        ) as cur:
            rows = await cur.fetchall()
    return [
        {
            "role": row["role"],
            "content": row["content"],
            "citations": json.loads(row["citations_json"]) if row["citations_json"] else None,
            "created_at": row["created_at"],
        }
        for row in rows
    ]
```

- [ ] **Step 4: Install pytest-asyncio**

```bash
pip install pytest-asyncio==0.23.6
```

- [ ] **Step 5: Create `pytest.ini` to enable asyncio mode**

```ini
[pytest]
asyncio_mode = auto
```

- [ ] **Step 6: Run — expect pass**

```bash
pytest tests/test_db.py -v
```

Expected: 4 tests PASSED.

- [ ] **Step 7: Commit**

```bash
git add backend/db.py tests/test_db.py pytest.ini
git commit -m "feat: add SQLite conversation store"
```

---

## Task 3: Stub retriever (`backend/retriever_stub.py`)

This is a placeholder that satisfies the same interface as the real retriever Plan 2 will build. It returns two hardcoded fake chunks so the chat endpoint works end-to-end without any Confluence credentials.

**Files:**
- Create: `backend/retriever_stub.py`

- [ ] **Step 1: Implement `backend/retriever_stub.py`**

```python
# Stub implementation of retrieve().
# Plan 2 replaces this with backend/retriever.py (real ChromaDB + Confluence ACL).
# Interface contract: returns list[dict] where each dict has:
#   document: str          — the text chunk
#   metadata: dict         — must contain page_id, page_title, page_url

_FAKE_CHUNKS = [
    {
        "document": "To deploy a new service, push your Docker image to the internal registry, update the Helm values file with the new tag, and open a PR to the infra-config repo for Platform team review.",
        "metadata": {
            "page_id": "stub-001",
            "page_title": "DevOps Runbook",
            "page_url": "https://example.atlassian.net/wiki/spaces/DEV/pages/stub-001",
        },
    },
    {
        "document": "New employees get read access to the ONBOARD and DEV spaces automatically. INFRA space requires manager approval via the Access Request Form.",
        "metadata": {
            "page_id": "stub-002",
            "page_title": "Onboarding Guide",
            "page_url": "https://example.atlassian.net/wiki/spaces/ONBOARD/pages/stub-002",
        },
    },
]

def retrieve(query: str, email: str, top_k: int = 5) -> list[dict]:
    """Stub: always returns the same two fake chunks regardless of query or email."""
    return _FAKE_CHUNKS[:top_k]
```

- [ ] **Step 2: Commit**

```bash
git add backend/retriever_stub.py
git commit -m "feat: add stub retriever (placeholder for Plan 2 RAG integration)"
```

---

## Task 4: FastAPI backend (`backend/main.py`)

**Files:**
- Create: `backend/main.py`

- [ ] **Step 1: Implement `backend/main.py`**

```python
import asyncio, json, os
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
from fastapi.staticfiles import StaticFiles
from pydantic import BaseModel
from dotenv import load_dotenv

from backend.db import init_db, save_message, get_history
from backend.retriever_stub import retrieve  # swap to backend.retriever once Plan 2 is done

load_dotenv()

@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
    yield

app = FastAPI(lifespan=lifespan)

class ChatRequest(BaseModel):
    email: str
    message: str

@app.post("/chat")
async def chat(req: ChatRequest):
    chunks = retrieve(req.message, req.email)

    if not chunks:
        async def no_access_stream():
            msg = "I couldn't find any documentation you have access to for that question."
            yield f"data: {json.dumps({'token': msg})}\n\n"
            yield "data: [DONE]\n\n"
        return StreamingResponse(no_access_stream(), media_type="text/event-stream")

    # Build citations from chunk metadata
    seen: set[str] = set()
    citations: list[dict] = []
    for chunk in chunks:
        pid = chunk["metadata"].get("page_id", "")
        if pid not in seen:
            seen.add(pid)
            citations.append({
                "title": chunk["metadata"].get("page_title", ""),
                "url": chunk["metadata"].get("page_url", ""),
            })

    # Stub response: echo the top chunk content as a streaming response
    # Plan 2 will replace this block with a real LLM call
    stub_answer = (
        f"[STUB — RAG not yet connected] "
        f"Based on '{chunks[0]['metadata']['page_title']}': "
        f"{chunks[0]['document'][:300]}"
    )

    await save_message(req.email, "user", req.message, None)

    full_response: list[str] = []

    async def stream():
        try:
            for word in stub_answer.split(" "):
                token = word + " "
                full_response.append(token)
                yield f"data: {json.dumps({'token': token})}\n\n"
                await asyncio.sleep(0.02)  # simulate streaming
            response_text = "".join(full_response)
            await save_message(req.email, "assistant", response_text, citations)
            yield f"data: {json.dumps({'citations': citations})}\n\n"
            yield "data: [DONE]\n\n"
        except Exception as exc:
            yield f"data: {json.dumps({'error': 'Something went wrong. Please try again.'})}\n\n"
            yield "data: [DONE]\n\n"

    return StreamingResponse(stream(), media_type="text/event-stream")

@app.get("/conversations/{email}")
async def conversations(email: str):
    rows = await get_history(email, limit=50)
    return {"messages": rows}

# Serve frontend static files
_frontend = os.path.join(os.path.dirname(__file__), "..", "frontend")
if os.path.isdir(_frontend):
    app.mount("/", StaticFiles(directory=_frontend, html=True), name="static")
```

- [ ] **Step 2: Smoke test — start server**

```bash
uvicorn backend.main:app --reload --port 8000
```

Expected: server starts, no import errors. Visit `http://localhost:8000/docs` — Swagger UI shows `/chat` and `/conversations/{email}`.

- [ ] **Step 3: Test `/conversations` returns empty list**

```bash
curl http://localhost:8000/conversations/test@example.com
```

Expected: `{"messages":[]}`

- [ ] **Step 4: Test `/chat` streams a response**

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","message":"how do I deploy?"}' \
  --no-buffer
```

Expected: SSE `data:` lines streaming word-by-word, ending with a `citations` event and `[DONE]`.

- [ ] **Step 5: Verify history is saved**

```bash
curl http://localhost:8000/conversations/test@example.com
```

Expected: JSON with 2 messages — one `user`, one `assistant` with `citations` array.

- [ ] **Step 6: Commit**

```bash
git add backend/main.py
git commit -m "feat: add FastAPI chat and conversations endpoints with stub retriever"
```

---

## Task 5: Frontend (`frontend/index.html`)

**Files:**
- Create: `frontend/index.html`

- [ ] **Step 1: Create `frontend/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>zinfo — IT Knowledge Assistant</title>
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  background: #0f1117; color: #e2e8f0;
  height: 100vh; display: flex; flex-direction: column; overflow: hidden;
}
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 24px; height: 56px;
  background: rgba(255,255,255,0.03);
  border-bottom: 1px solid rgba(255,255,255,0.07); flex-shrink: 0;
}
.logo { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 1.05rem; letter-spacing: -0.02em; }
.logo-icon { width: 30px; height: 30px; background: linear-gradient(135deg,#6366f1,#8b5cf6); border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: 0.85rem; }
.user-pill { display: flex; align-items: center; gap: 8px; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); border-radius: 20px; padding: 5px 12px 5px 8px; font-size: 0.82rem; color: #94a3b8; }
.avatar { width: 22px; height: 22px; background: linear-gradient(135deg,#6366f1,#06b6d4); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.65rem; font-weight: 700; color: white; }
.messages { flex: 1; overflow-y: auto; padding: 32px 0; scroll-behavior: smooth; }
.messages::-webkit-scrollbar { width: 4px; }
.messages::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 2px; }
.msg-row { display: flex; padding: 6px 24px; max-width: 860px; margin: 0 auto; width: 100%; animation: fadeIn 0.2s ease; }
@keyframes fadeIn { from { opacity:0; transform:translateY(6px); } to { opacity:1; transform:translateY(0); } }
.msg-row.user { justify-content: flex-end; }
.bubble { max-width: 72%; padding: 12px 16px; border-radius: 18px; font-size: 0.92rem; line-height: 1.65; }
.user .bubble { background: linear-gradient(135deg,rgba(99,102,241,0.55),rgba(139,92,246,0.45)); border: 1px solid rgba(99,102,241,0.4); border-bottom-right-radius: 4px; color: #e0e7ff; }
.assistant .bubble-wrap { display: flex; flex-direction: column; gap: 10px; max-width: 80%; }
.assistant .bubble { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.09); border-bottom-left-radius: 4px; color: #cbd5e1; max-width: 100%; white-space: pre-wrap; }
.citations { display: flex; flex-wrap: wrap; gap: 7px; padding-left: 2px; }
.cite-pill { display: inline-flex; align-items: center; gap: 5px; padding: 4px 11px; background: rgba(99,102,241,0.12); border: 1px solid rgba(99,102,241,0.28); border-radius: 20px; font-size: 0.76rem; color: #a5b4fc; text-decoration: none; transition: background 0.15s; }
.cite-pill:hover { background: rgba(99,102,241,0.22); color: #c7d2fe; }
.typing-row { display: flex; padding: 6px 24px; max-width: 860px; margin: 0 auto; width: 100%; }
.typing-bubble { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.09); border-radius: 18px; border-bottom-left-radius: 4px; padding: 13px 18px; display: flex; gap: 5px; align-items: center; }
.dot { width: 7px; height: 7px; border-radius: 50%; background: #64748b; animation: bounce 1.2s infinite; }
.dot:nth-child(2) { animation-delay: 0.15s; }
.dot:nth-child(3) { animation-delay: 0.3s; }
@keyframes bounce { 0%,60%,100% { transform:translateY(0); opacity:0.5; } 30% { transform:translateY(-5px); opacity:1; } }
.inputbar { padding: 16px 24px 20px; flex-shrink: 0; }
.input-wrap { max-width: 860px; margin: 0 auto; display: flex; gap: 10px; align-items: flex-end; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.12); border-radius: 16px; padding: 10px 12px 10px 18px; transition: border-color 0.15s; }
.input-wrap:focus-within { border-color: rgba(99,102,241,0.5); box-shadow: 0 0 0 3px rgba(99,102,241,0.08); }
.chat-input { flex: 1; background: transparent; border: none; outline: none; color: #e2e8f0; font-size: 0.93rem; line-height: 1.5; resize: none; min-height: 24px; max-height: 120px; font-family: inherit; }
.chat-input::placeholder { color: #475569; }
.send-btn { width: 36px; height: 36px; background: linear-gradient(135deg,#6366f1,#8b5cf6); border: none; border-radius: 10px; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: opacity 0.15s, transform 0.1s; color: white; font-size: 0.95rem; }
.send-btn:hover { opacity: 0.85; transform: scale(1.05); }
.send-btn:active { transform: scale(0.96); }
.input-hint { text-align: center; font-size: 0.72rem; color: #334155; margin-top: 8px; max-width: 860px; margin-left: auto; margin-right: auto; }
#gate-screen { display: flex; align-items: center; justify-content: center; height: 100vh; flex-direction: column; gap: 16px; }
.gate-input { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.12); border-radius: 10px; padding: 10px 14px; color: #e2e8f0; font-size: 0.93rem; outline: none; width: 100%; transition: border-color 0.15s; }
.gate-input:focus { border-color: rgba(99,102,241,0.5); }
.gate-btn { background: linear-gradient(135deg,#6366f1,#8b5cf6); border: none; border-radius: 10px; padding: 10px 22px; color: white; font-weight: 600; cursor: pointer; font-size: 0.93rem; transition: opacity 0.15s; }
.gate-btn:hover { opacity: 0.85; }
</style>
</head>
<body>

<!-- Email gate -->
<div id="gate-screen">
  <div style="font-size:2rem;font-weight:800;letter-spacing:-0.04em">⚡ zinfo</div>
  <p style="color:#475569;font-size:0.9rem">Your IT knowledge assistant</p>
  <div style="display:flex;gap:8px;width:100%;max-width:380px;margin-top:8px">
    <input id="email-input" class="gate-input" type="email" placeholder="you@company.com"
      onkeydown="if(event.key==='Enter')enterChat()">
    <button class="gate-btn" onclick="enterChat()">Continue</button>
  </div>
  <div id="gate-error" style="color:#f87171;font-size:0.82rem;min-height:18px"></div>
</div>

<!-- Chat screen -->
<div id="chat-screen" style="display:none;height:100vh;flex-direction:column">
  <div class="topbar">
    <div class="logo"><div class="logo-icon">⚡</div>zinfo</div>
    <div style="font-size:0.78rem;color:#475569;letter-spacing:0.02em">IT Knowledge Assistant</div>
    <div class="user-pill">
      <div class="avatar" id="avatar-initials">?</div>
      <span id="user-email-label"></span>
    </div>
  </div>
  <div class="messages" id="messages"></div>
  <div class="inputbar">
    <div class="input-wrap">
      <textarea id="chat-input" class="chat-input" placeholder="Ask about the IT system…" rows="1"
        oninput="this.style.height='auto';this.style.height=this.scrollHeight+'px'"
        onkeydown="if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();sendMessage()}"></textarea>
      <button class="send-btn" onclick="sendMessage()">↑</button>
    </div>
    <div class="input-hint">Answers grounded in your Confluence docs · Sources shown below each response</div>
  </div>
</div>

<script>
let currentEmail = sessionStorage.getItem('zinfo_email') || '';
if (currentEmail) showChat(currentEmail);

function enterChat() {
  const val = document.getElementById('email-input').value.trim();
  const err = document.getElementById('gate-error');
  if (!val || !val.includes('@')) { err.textContent = 'Please enter a valid email.'; return; }
  err.textContent = '';
  sessionStorage.setItem('zinfo_email', val);
  showChat(val);
}

async function showChat(email) {
  currentEmail = email;
  document.getElementById('gate-screen').style.display = 'none';
  const cs = document.getElementById('chat-screen');
  cs.style.display = 'flex';
  document.getElementById('user-email-label').textContent = email;
  document.getElementById('avatar-initials').textContent = email[0].toUpperCase();

  const res = await fetch('/conversations/' + encodeURIComponent(email));
  const data = await res.json();
  for (const msg of data.messages) appendMessage(msg.role, msg.content, msg.citations);
  scrollBottom();
}

function appendMessage(role, content, citations) {
  const wrap = document.getElementById('messages');
  const row = document.createElement('div');
  row.className = 'msg-row ' + role;
  if (role === 'user') {
    row.innerHTML = '<div class="bubble">' + esc(content) + '</div>';
  } else {
    const citeHtml = (citations && citations.length)
      ? '<div class="citations">' + citations.map(c =>
          '<a class="cite-pill" href="' + esc(c.url) + '" target="_blank">📄 ' + esc(c.title) + '</a>'
        ).join('') + '</div>'
      : '';
    row.innerHTML = '<div class="bubble-wrap"><div class="bubble">' + esc(content) + '</div>' + citeHtml + '</div>';
  }
  wrap.appendChild(row);
  return row;
}

function addTyping() {
  const wrap = document.getElementById('messages');
  const row = document.createElement('div');
  row.className = 'typing-row'; row.id = 'typing';
  row.innerHTML = '<div class="typing-bubble"><div class="dot"></div><div class="dot"></div><div class="dot"></div></div>';
  wrap.appendChild(row); scrollBottom();
  return row;
}

async function sendMessage() {
  const input = document.getElementById('chat-input');
  const text = input.value.trim();
  if (!text) return;
  input.value = ''; input.style.height = 'auto';

  appendMessage('user', text, null);
  const typing = addTyping();
  scrollBottom();

  const wrap = document.getElementById('messages');
  const row = document.createElement('div');
  row.className = 'msg-row assistant';
  const bw = document.createElement('div'); bw.className = 'bubble-wrap';
  const bubble = document.createElement('div'); bubble.className = 'bubble';
  bw.appendChild(bubble); row.appendChild(bw);

  typing.remove();
  wrap.appendChild(row);

  const resp = await fetch('/chat', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({email: currentEmail, message: text}),
  });

  const reader = resp.body.getReader();
  const dec = new TextDecoder();
  let buf = '';

  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += dec.decode(value, {stream: true});
    const lines = buf.split('\n');
    buf = lines.pop();
    for (const line of lines) {
      if (!line.startsWith('data: ')) continue;
      const raw = line.slice(6).trim();
      if (raw === '[DONE]') continue;
      try {
        const p = JSON.parse(raw);
        if (p.token) { bubble.textContent += p.token; scrollBottom(); }
        if (p.citations && p.citations.length) {
          const cd = document.createElement('div'); cd.className = 'citations';
          cd.innerHTML = p.citations.map(c =>
            '<a class="cite-pill" href="' + esc(c.url) + '" target="_blank">📄 ' + esc(c.title) + '</a>'
          ).join('');
          bw.appendChild(cd); scrollBottom();
        }
        if (p.error) { bubble.textContent = p.error; }
      } catch {}
    }
  }
}

function scrollBottom() { const m = document.getElementById('messages'); m.scrollTop = m.scrollHeight; }
function esc(s) { return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }
</script>
</body>
</html>
```

- [ ] **Step 2: Restart server and open browser**

```bash
uvicorn backend.main:app --reload --port 8000
```

Open `http://localhost:8000` — verify email gate screen appears.

- [ ] **Step 3: Test email gate**

Enter any email address (e.g. `demo@company.com`). Verify chat screen appears with the email shown in the top-right pill.

- [ ] **Step 4: Test chat flow**

Type a question and press Enter. Verify:
- Typing indicator (three dots) appears
- Response streams in word-by-word
- Citation pills appear below the response linking to the stub URLs
- Input clears after sending

- [ ] **Step 5: Test history persistence**

Refresh the page. Enter the same email. Verify the previous conversation is restored.

- [ ] **Step 6: Commit**

```bash
git add frontend/index.html
git commit -m "feat: add chat frontend with email gate, SSE streaming, and citation pills"
```

---

## Task 6: Run all Plan 1 tests

- [ ] **Step 1: Run full test suite**

```bash
pytest tests/ -v
```

Expected: all tests pass (currently only `tests/test_db.py`).

- [ ] **Step 2: Final Plan 1 commit**

```bash
git add -A
git commit -m "chore: Plan 1 complete — UI, API, and storage working with stub retriever"
```

---

## Integration note (for after Plan 2 is complete)

When Plan 2 finishes, update **one line** in `backend/main.py`:

```python
# Before (Plan 1 stub):
from backend.retriever_stub import retrieve

# After (Plan 2 real RAG):
from backend.retriever import retrieve
```

Also uncomment the `/webhook` and `/index` routes from Plan 2's `main_additions.py` snippet (see Plan 2).
